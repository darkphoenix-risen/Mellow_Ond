# spring 코딩법: 순서
# spring 인터넷 기본주소: http://localhost:8080
# 1) vo(db 테이블과 비슷) -> 2) mapper(sql) -> 3) service(업무용 함수) -> 4) controller(인터넷주소+화면) -> 5) jsp(화면)
# vo: db 의 테이블 구조와 같은 클래스(컬럼==필드, 테이블명==클래스명)
# 최초 1번만 작성
# 목적: 테이블의 데이터를 자바에서 임시 저장할 목적으로 사용합니다.
# 화면에 정보 보여주기 -> 1) db에서 데이터 가져온다 
                       2) vo 클래스에 1개 행을 넣는다(관리가 편함, 목적별로 폴더/파일을 분리하면 수정하기 편함)
                       3) jsp 에서 vo 내용을 표시한다.

# 2) mapper : sql 을 작성하는 것, 함수를 실행하면 sql 이 실행되는 구조
       - 함수명을 정함 
       - 그 함수에 sql 을 작성해 놓음

# 3) 페이징 이론
-- 부서 사원명 검색
SELECT * FROM TB_DEPT WHERE DNAME LIKE '%'||''||'%'
OFFSET 3 ROWS
FETCH NEXT 3 ROWS ONLY;

-- 위의 sql 에 페이징 기능을 추가합시다.
-- 화면에 몇개를 보여줄건가? 3개
-- 1 페이지=> 3개씩(0~2순번), 현재페이지번호 * 화면에 보일개수: 0*3=> 오프셋번호: 0
-- 2 페이지=> 3개씩(3순번)    1*3=> 오프셋번호: 3

# 4) 부서 총개수 구하기
-- 부서 테이블의 총개수 구하기(단, 검색하면 검색을 고려해서 총개수 구하기)
-- 왜? jquery 페이지 플러그인(자동으로 그려줌): 총개수를 줘야함
SELECT COUNT(*) FROM TB_DEPT WHERE DNAME LIKE '%'||''||'%' 

# 5) 페이징 플러그인 사용법
    /* 페이징 처리 */
    $('#pagination').twbsPagination({
        totalPages: 총페이지수,
        startPage:현재페이지번호,
        visiblePages: 화면에 보일개수,
        initiateStartPageClick: false,
        onPageClick: function (event, page) {
            검색함수명(page)
        }
    });